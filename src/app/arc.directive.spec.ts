import { ArcDirective } from './arc.directive';

describe('ArcDirective', () => {
  it('should create an instance', () => {
    const directive = new ArcDirective();
    expect(directive).toBeTruthy();
  });
});
